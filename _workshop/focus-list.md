# metrics hygiene - make the telemetry trustworthy again

- [ ] 🚧 Given prime-agent's source and a fresh tiny probe run, When we count `agent_start`/`agent_end` events the way labnote 03 did, Then the extra `agent_end`'s origin is identified and the counting recipe (or the lab note's numbers) is corrected - closing backlog item 4 (as graduated).
- [ ] Given a child spawned then collected in a fresh probe, When the roster status is read after `collect()` returns `done`, Then the `cancelled`-after-done quirk's cause is identified from journal evidence and documented - closing backlog item 3 (as graduated).
